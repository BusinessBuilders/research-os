Selection checkbox — fills with brand periwinkle when checked. Drives need selection on Gap Analysis and product selection on Results.

```jsx
<Checkbox checked={selected.has(id)} onCheckedChange={() => toggle(id)} />
```

Align to the first text line with `margin-top` when paired with multi-line labels.
