Single-line text field on the navy canvas — focus ring is periwinkle.

```jsx
<label className="ros-label">Budget (optional)</label>
<Input type="number" placeholder="500" />
<Input mono placeholder="project-eve-humanoid-robot" />
```

Pair with `.ros-label` and `.ros-hint`. Use `mono` for part numbers, prices, and wiki slugs.
