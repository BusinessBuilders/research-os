Mint cash-position pill — the only off-gradient accent in the UI. Signals available budget on New Research and in decision summaries.

```jsx
<BudgetPill amount={4200} />        // → "$4,200 available"
<BudgetPill amount={500} label="remaining" />
```

Reserve mint for budget/cash context only — don't reuse it for generic highlights.
