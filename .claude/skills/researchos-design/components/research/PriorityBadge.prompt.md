Priority pill shown inline next to each identified need on Gap Analysis.

```jsx
<PriorityBadge priority="critical" />
```

Maps to the semantic palette: `critical`=danger/red, `important`=info/blue, `nice-to-have`=neutral.
